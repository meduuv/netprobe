# IPv6 diagnostics

When IPv6 is available, diagnostics should preserve the address family in their output rather than collapsing IPv4 and IPv6 results together.

Record the protocol used for each observation so operators can distinguish DNS, routing, and service differences between address families.
